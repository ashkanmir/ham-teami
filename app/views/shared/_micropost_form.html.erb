<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "هر چی میخوای با بقیخ به اشتراک بزاری اینجا بنویس.............." %>
  </div>
  <%= f.submit "بفرست", class: "btn btn-primary" %>
<% end %>

